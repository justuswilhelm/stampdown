machine:
  environment:
    DATABASE_URL: "postgres://ubuntu@localhost:5432/circle_test"
    SECRET_KEY: 'CI'
    DEBUG: 'test'
test:
    override:
        - cd .. && cookiecutter django-template --no-input
        - cd .. && mv ~/django-template ~/_django-template && mv ~/django_project ~/django-template
        - pip install -r requirements.txt
        - script/bootstrap
        - script/test
        - ./manage.py test
        - script/style
        - npm install
        - ./manage.py collectstatic --no-input
