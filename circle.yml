machine:
  environment:
    DATABASE_URL: "postgres://ubuntu@localhost:5432/circle_test"
    SECRET_KEY: 'CI'
    DEBUG: 'test'
test:
    override:
        - cd ..; cookiecutter django-template --no-input
        - cd ~/django_project; pip install -r requirements.txt && script/bootstrap
        - cd ~/django_project && script/test && ./manage.py test
